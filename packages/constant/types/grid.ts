/**
 * 环境枚举
 * @desktop web桌面
 * @mobile 移动端
 */
export enum BROWSER_ENV {
    desktop = 'desktop',
    mobile = 'mobile',
}

export enum BROWSER_ENV_GRID_COLUMN {
    desktop = 4,
    mobile = 2,
}
export enum ENV_ENUM {
    desktop = 'desktop',
    mobile = 'mobile'
}

export enum EDIT_TYPE {
    resize = 'resize',
    normal = 'normal',
    messagge = 'messagge',
    edit = 'edit',
    select = 'select'
}


// export enum MESSAGE_TYPE {
//     edit = 'edit',
//     updateHandler = 'updateHandler'
// }


/** 小组件类型
 * @link 普通2×2和4×4组件
 * @media 2×2 图片、视频小组件
 * @richText 2×2 富文本组件
 * @sectionHeader 4×1章节标题
 *  */
